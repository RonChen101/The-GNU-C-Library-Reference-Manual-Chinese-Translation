目录改动。

新：

    3.2.3.3 Portable Memory Allocation
    3.2.3.4 Freeing Memory Allocated with malloc
    3.2.3.5 Changing the Size of a Block
    3.2.3.6 Allocating Cleared Space
    3.2.3.7 Allocating Aligned Memory Blocks
    3.2.3.8 Malloc Tunable Parameters
    3.2.3.9 Heap Consistency Checking
    3.2.3.10 Statistics for Memory Allocation with malloc
    3.2.3.11 Summary of malloc-Related Functions

旧：

    3.2.3.3 Freeing Memory Allocated with malloc
    3.2.3.4 Changing the Size of a Block
    3.2.3.5 Allocating Cleared Space
    3.2.3.6 Allocating Aligned Memory Blocks
    3.2.3.7 Malloc Tunable Parameters
    3.2.3.8 Heap Consistency Checking
    3.2.3.9 Statistics for Memory Allocation with malloc
    3.2.3.10 Summary of malloc-Related Functions

---

目录改动。

新：

    37.4 Indirect Functions
        37.4.1 IFUNC Resolver Calling Conventions
        37.4.2 When IFUNC Resolvers Run
        37.4.3 Supported Functionality in IFUNC Resolvers
    37.5 Avoiding Unexpected Issues With Dynamic Linking
        37.5.1 Restricted Dynamic Linker Features
        37.5.2 Producing Matching Binaries
        37.5.3 Checking Binaries
        37.5.4 Run-time Considerations

旧：

    37.4 Avoiding Unexpected Issues With Dynamic Linking
        37.4.1 Restricted Dynamic Linker Features
        37.4.2 Producing Matching Binaries
        37.4.3 Checking Binaries
        37.4.4 Run-time Considerations

---

目录改动

新：

    39.1 System-wide Tunables
    39.2 Tunable names
    39.3 Memory Allocation Tunables
    39.4 Dynamic Linking Tunables
    39.5 POSIX Thread Tunables
    39.6 Hardware Capability Tunables
    39.7 Memory Related Tunables
    39.8 gmon Tunables
    39.9 ELF Tunables

旧：

    39.1 Tunable names
    39.2 Memory Allocation Tunables
    39.3 Dynamic Linking Tunables
    39.4 POSIX Thread Tunables
    39.5 Hardware Capability Tunables
    39.6 Memory Related Tunables
    39.7 gmon Tunables

---

1.3.4 Feature Test Macros改动

新：

    If the macro is not defined it currently defaults to 32, but this default is planned to change in the future. This is because _TIME_BITS=64 (required for time_t Y2038 safety) can only be used together with _FILE_OFFSET_BITS=64. When systems migrate to 64-bit time_t by default, _FILE_OFFSET_BITS will also need to default to 64, even for applications that do not handle large files. Applications should not rely on the current default.

旧：

    If the macro is not defined it currently defaults to 32, but this default is planned to change due to a need to update time_t for Y2038 safety, and applications should not rely on the default.

---

2.1 Checking for Errors改动

新：

    The error code values are all positive integers and are all distinct, so they can be used as labels in a switch statement. Your program should not make any other assumptions about the specific values of these symbolic constants. There are two exceptions to this rule:
        The values of EAGAIN and EWOULDBLOCK are equal on every supported operating system.
        The values of ENOTSUP and EOPNOTSUPP are equal on some supported operating systems, for example GNU/Linux.
    To make your program portable, you should check for both codes and treat them the same.

旧：

    The error code values are all positive integers and are all distinct, with one exception: EWOULDBLOCK and EAGAIN are the same. Since the values are distinct, you can use them as labels in a switch statement; just don’t use both EWOULDBLOCK and EAGAIN. Your program should not make any other assumptions about the specific values of these symbolic constants.

---

由于目前只翻译到第一章，所以后面的改动暂不处理。哈哈